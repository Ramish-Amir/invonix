rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simplified rules - allow all authenticated users for now
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
